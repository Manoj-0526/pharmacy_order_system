@page
@model Pharmacy_order_system.Pages.IndexModel
@{
    ViewData["Title"] = "Welcome";
}
<div class="hero-panel text-center">
    <h1>Pharmacy Ordering System</h1>
    <p>Secure online medicine ordering with prescription validation.</p>
    <div class="d-flex justify-content-center gap-2 mt-3">
        <a class="btn btn-warning btn-lg" asp-page="/Account/Login">Login</a>
        <a class="btn btn-outline-dark btn-lg" asp-page="/Account/Register">Register</a>
    </div>
</div>
